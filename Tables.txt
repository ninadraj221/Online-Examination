CREATE TABLE  "QUESTIONS" 
   (	"QID" VARCHAR2(20) NOT NULL ENABLE, 
	"QNAME" VARCHAR2(100), 
	"OPTION1" VARCHAR2(100), 
	"OPTION2" VARCHAR2(100), 
	"OPTION3" VARCHAR2(100), 
	"OPTION4" VARCHAR2(100), 
	"CORRECT_ANS" VARCHAR2(100) NOT NULL ENABLE, 
	"TID" VARCHAR2(20), 
	 CONSTRAINT "QID" PRIMARY KEY ("QID") ENABLE
   ) ;ALTER TABLE  "QUESTIONS" ADD CONSTRAINT "TOPIC_FK" FOREIGN KEY ("TID")
	  REFERENCES  "TOPIC" ("TID") ENABLE;


CREATE TABLE  "REPORTCARD" 
   (	"RID" VARCHAR2(20) NOT NULL ENABLE, 
	"USERID" VARCHAR2(20), 
	"TID" VARCHAR2(20), 
	"MARKS" NUMBER, 
	"REMARKS" VARCHAR2(20), 
	 CONSTRAINT "REPORTCARD_PK" PRIMARY KEY ("RID") ENABLE
   ) ;ALTER TABLE  "REPORTCARD" ADD CONSTRAINT "REPORTCARD_TOPIC_FK" FOREIGN KEY ("TID")
	  REFERENCES  "TOPIC" ("TID") ENABLE;ALTER TABLE  "REPORTCARD" ADD CONSTRAINT "REPORTCARD_USER_FK" FOREIGN KEY ("USERID")
	  REFERENCES  "USERS" ("USERID") ENABLE;

CREATE TABLE  "TOPIC" 
   (	"TID" VARCHAR2(20) NOT NULL ENABLE, 
	"TNAME" VARCHAR2(20) NOT NULL ENABLE, 
	"TLEVEL" NUMBER NOT NULL ENABLE, 
	 CONSTRAINT "TOPIC_PK" PRIMARY KEY ("TID") ENABLE
   ) ;


CREATE TABLE  "USERS" 
   (	"USERID" VARCHAR2(20) NOT NULL ENABLE, 
	"EMAIL" VARCHAR2(40) NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(20) NOT NULL ENABLE, 
	"CITY" VARCHAR2(20) NOT NULL ENABLE, 
	"MOBILE_NUMBER" NUMBER(11,0) NOT NULL ENABLE, 
	"COLLEGE" VARCHAR2(50) NOT NULL ENABLE, 
	"FULLNAME" VARCHAR2(50) NOT NULL ENABLE, 
	 CONSTRAINT "USERS_PK" PRIMARY KEY ("USERID") ENABLE, 
	 UNIQUE ("EMAIL") ENABLE, 
	 UNIQUE ("MOBILE_NUMBER") ENABLE
   ) ;
